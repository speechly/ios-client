# IdentityClient.IdentityClientError

Errors returned by the client.

``` swift
public enum IdentityClientError
```

## Inheritance

`Error`

## Enumeration Cases

### `invalidTokenPayload`

The error returned if the API returns an invalid access token.

``` swift
case invalidTokenPayload
```
